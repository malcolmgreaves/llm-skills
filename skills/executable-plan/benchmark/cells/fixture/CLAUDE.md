# cells

Python 3.12, standard library only. No third-party dependencies.

- Run the tests: `python3 -m unittest discover -s tests -q`
- Run the CLI: `python3 -m cells ...`

Every change keeps the test suite passing and adds tests for its new behavior.
README.md documents the behavior; update it with every feature.
